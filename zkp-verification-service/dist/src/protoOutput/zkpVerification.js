// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v6.32.1
// source: zkpVerification.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { makeGenericClientConstructor, } from "@grpc/grpc-js";
export const protobufPackage = "zkpVerification";
function createBaseVerifyClauseInclusionRequest() {
    return { agreementId: "", clauseSetHash: "", commitment: "" };
}
export const VerifyClauseInclusionRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.agreementId !== "") {
            writer.uint32(10).string(message.agreementId);
        }
        if (message.clauseSetHash !== "") {
            writer.uint32(18).string(message.clauseSetHash);
        }
        if (message.commitment !== "") {
            writer.uint32(26).string(message.commitment);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVerifyClauseInclusionRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.agreementId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.clauseSetHash = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.commitment = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            agreementId: isSet(object.agreementId) ? globalThis.String(object.agreementId) : "",
            clauseSetHash: isSet(object.clauseSetHash) ? globalThis.String(object.clauseSetHash) : "",
            commitment: isSet(object.commitment) ? globalThis.String(object.commitment) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.agreementId !== "") {
            obj.agreementId = message.agreementId;
        }
        if (message.clauseSetHash !== "") {
            obj.clauseSetHash = message.clauseSetHash;
        }
        if (message.commitment !== "") {
            obj.commitment = message.commitment;
        }
        return obj;
    },
    create(base) {
        return VerifyClauseInclusionRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseVerifyClauseInclusionRequest();
        message.agreementId = (_a = object.agreementId) !== null && _a !== void 0 ? _a : "";
        message.clauseSetHash = (_b = object.clauseSetHash) !== null && _b !== void 0 ? _b : "";
        message.commitment = (_c = object.commitment) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
function createBaseVerifyClauseInclusionResponse() {
    return { success: false, message: "", isValid: false };
}
export const VerifyClauseInclusionResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.message !== "") {
            writer.uint32(18).string(message.message);
        }
        if (message.isValid !== false) {
            writer.uint32(24).bool(message.isValid);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVerifyClauseInclusionResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.isValid = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            isValid: isSet(object.isValid) ? globalThis.Boolean(object.isValid) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.isValid !== false) {
            obj.isValid = message.isValid;
        }
        return obj;
    },
    create(base) {
        return VerifyClauseInclusionResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseVerifyClauseInclusionResponse();
        message.success = (_a = object.success) !== null && _a !== void 0 ? _a : false;
        message.message = (_b = object.message) !== null && _b !== void 0 ? _b : "";
        message.isValid = (_c = object.isValid) !== null && _c !== void 0 ? _c : false;
        return message;
    },
};
function createBaseBGExpiryCheckRequest() {
    return { bgExpiry: "", POEndDate: "", NDays: "", bgExpiryHash: "" };
}
export const BGExpiryCheckRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.bgExpiry !== "") {
            writer.uint32(10).string(message.bgExpiry);
        }
        if (message.POEndDate !== "") {
            writer.uint32(18).string(message.POEndDate);
        }
        if (message.NDays !== "") {
            writer.uint32(26).string(message.NDays);
        }
        if (message.bgExpiryHash !== "") {
            writer.uint32(34).string(message.bgExpiryHash);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBGExpiryCheckRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.bgExpiry = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.POEndDate = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.NDays = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.bgExpiryHash = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            bgExpiry: isSet(object.bgExpiry) ? globalThis.String(object.bgExpiry) : "",
            POEndDate: isSet(object.POEndDate) ? globalThis.String(object.POEndDate) : "",
            NDays: isSet(object.NDays) ? globalThis.String(object.NDays) : "",
            bgExpiryHash: isSet(object.bgExpiryHash) ? globalThis.String(object.bgExpiryHash) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.bgExpiry !== "") {
            obj.bgExpiry = message.bgExpiry;
        }
        if (message.POEndDate !== "") {
            obj.POEndDate = message.POEndDate;
        }
        if (message.NDays !== "") {
            obj.NDays = message.NDays;
        }
        if (message.bgExpiryHash !== "") {
            obj.bgExpiryHash = message.bgExpiryHash;
        }
        return obj;
    },
    create(base) {
        return BGExpiryCheckRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseBGExpiryCheckRequest();
        message.bgExpiry = (_a = object.bgExpiry) !== null && _a !== void 0 ? _a : "";
        message.POEndDate = (_b = object.POEndDate) !== null && _b !== void 0 ? _b : "";
        message.NDays = (_c = object.NDays) !== null && _c !== void 0 ? _c : "";
        message.bgExpiryHash = (_d = object.bgExpiryHash) !== null && _d !== void 0 ? _d : "";
        return message;
    },
};
function createBaseBGExpiryCheckResponse() {
    return { success: false, message: "", isValid: false };
}
export const BGExpiryCheckResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.message !== "") {
            writer.uint32(18).string(message.message);
        }
        if (message.isValid !== false) {
            writer.uint32(24).bool(message.isValid);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBGExpiryCheckResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.isValid = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            isValid: isSet(object.isValid) ? globalThis.Boolean(object.isValid) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.isValid !== false) {
            obj.isValid = message.isValid;
        }
        return obj;
    },
    create(base) {
        return BGExpiryCheckResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseBGExpiryCheckResponse();
        message.success = (_a = object.success) !== null && _a !== void 0 ? _a : false;
        message.message = (_b = object.message) !== null && _b !== void 0 ? _b : "";
        message.isValid = (_c = object.isValid) !== null && _c !== void 0 ? _c : false;
        return message;
    },
};
function createBaseAmountWithinRangeRequest() {
    return { invoiceTotal: "", poBalance: "", poBalanceHash: "" };
}
export const AmountWithinRangeRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.invoiceTotal !== "") {
            writer.uint32(10).string(message.invoiceTotal);
        }
        if (message.poBalance !== "") {
            writer.uint32(18).string(message.poBalance);
        }
        if (message.poBalanceHash !== "") {
            writer.uint32(26).string(message.poBalanceHash);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAmountWithinRangeRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.invoiceTotal = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.poBalance = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.poBalanceHash = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            invoiceTotal: isSet(object.invoiceTotal) ? globalThis.String(object.invoiceTotal) : "",
            poBalance: isSet(object.poBalance) ? globalThis.String(object.poBalance) : "",
            poBalanceHash: isSet(object.poBalanceHash) ? globalThis.String(object.poBalanceHash) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.invoiceTotal !== "") {
            obj.invoiceTotal = message.invoiceTotal;
        }
        if (message.poBalance !== "") {
            obj.poBalance = message.poBalance;
        }
        if (message.poBalanceHash !== "") {
            obj.poBalanceHash = message.poBalanceHash;
        }
        return obj;
    },
    create(base) {
        return AmountWithinRangeRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseAmountWithinRangeRequest();
        message.invoiceTotal = (_a = object.invoiceTotal) !== null && _a !== void 0 ? _a : "";
        message.poBalance = (_b = object.poBalance) !== null && _b !== void 0 ? _b : "";
        message.poBalanceHash = (_c = object.poBalanceHash) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
function createBaseAmountWithinRangeResponse() {
    return { success: false, message: "", isValid: false };
}
export const AmountWithinRangeResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.message !== "") {
            writer.uint32(18).string(message.message);
        }
        if (message.isValid !== false) {
            writer.uint32(24).bool(message.isValid);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAmountWithinRangeResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.isValid = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            isValid: isSet(object.isValid) ? globalThis.Boolean(object.isValid) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.isValid !== false) {
            obj.isValid = message.isValid;
        }
        return obj;
    },
    create(base) {
        return AmountWithinRangeResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseAmountWithinRangeResponse();
        message.success = (_a = object.success) !== null && _a !== void 0 ? _a : false;
        message.message = (_b = object.message) !== null && _b !== void 0 ? _b : "";
        message.isValid = (_c = object.isValid) !== null && _c !== void 0 ? _c : false;
        return message;
    },
};
export const ZKPVerificationServiceService = {
    verifyClauseInclusion: {
        path: "/zkpVerification.ZKPVerificationService/VerifyClauseInclusion",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(VerifyClauseInclusionRequest.encode(value).finish()),
        requestDeserialize: (value) => VerifyClauseInclusionRequest.decode(value),
        responseSerialize: (value) => Buffer.from(VerifyClauseInclusionResponse.encode(value).finish()),
        responseDeserialize: (value) => VerifyClauseInclusionResponse.decode(value),
    },
    bgExpiryCheck: {
        path: "/zkpVerification.ZKPVerificationService/BGExpiryCheck",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(BGExpiryCheckRequest.encode(value).finish()),
        requestDeserialize: (value) => BGExpiryCheckRequest.decode(value),
        responseSerialize: (value) => Buffer.from(BGExpiryCheckResponse.encode(value).finish()),
        responseDeserialize: (value) => BGExpiryCheckResponse.decode(value),
    },
    amountWithinRange: {
        path: "/zkpVerification.ZKPVerificationService/AmountWithinRange",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(AmountWithinRangeRequest.encode(value).finish()),
        requestDeserialize: (value) => AmountWithinRangeRequest.decode(value),
        responseSerialize: (value) => Buffer.from(AmountWithinRangeResponse.encode(value).finish()),
        responseDeserialize: (value) => AmountWithinRangeResponse.decode(value),
    },
};
export const ZKPVerificationServiceClient = makeGenericClientConstructor(ZKPVerificationServiceService, "zkpVerification.ZKPVerificationService");
function isSet(value) {
    return value !== null && value !== undefined;
}
