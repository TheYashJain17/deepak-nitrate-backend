services:

  api-gateway:
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "9009:9009"
    restart: unless-stopped
    networks:
      - microservices-net

networks:
  microservices-net:
    external: true